# 이 파일(.env.example)은 실제 값이 없으니 Git에 올려도 됩니다.
# 사용법: 이 파일을 복사해서 같은 폴더에 ".env"라는 이름으로 저장하고,
#        아래 값을 공공데이터포털에서 발급받은 실제 인증키로 바꾸세요.
# .env 파일 자체는 워크스페이스 루트 .gitignore에서 이미 제외되므로 Git에 올라가지 않습니다.

DATA_GO_KR_SERVICE_KEY=여기에_발급받은_인증키_Decoding값을_붙여넣기
